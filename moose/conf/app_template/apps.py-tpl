${unicode_literals}from moose.apps import AppConfig


class ${camel_case_app_name}Config(AppConfig):
    name = '${app_name}'
    verbose_name = u''

    def ready(self):
        self.register('ActionClass', 'action-name')
